//! Port of `graph/properties/IPropertyHolder.swift`.
//!
//! Every holder in the port owns a [`PropertyMap`](super::map_property_holder::PropertyMap);
//! the protocol's methods are that type's methods.

pub use super::map_property_holder::PropertyMap;
