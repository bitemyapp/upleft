//! Port of `App/DocumentWindowController+AssetDoctor.swift`. Not ported yet.

use crate::app::document_window_controller::DocumentWindowController;
use crate::panels::asset_doctor_view::AssetDoctorView;

impl DocumentWindowController {
    /// `configureAssetDoctor(_:)`.
    pub fn configure_asset_doctor(&self, _view: &AssetDoctorView) {
        // PORT: DocumentWindowController+AssetDoctor.swift
    }
}
