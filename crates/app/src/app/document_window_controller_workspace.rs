//! Port of `App/DocumentWindowController+Workspace.swift`. Not ported yet.

/// The extension's associated-object state (`workspaceIndex`, `workspaceSidebar`, `workspaceSearch`, `workspaceGraph`), held by the
/// controller as `workspace_state()`.
// PORT: DocumentWindowController+Workspace.swift fills this in.
#[derive(Default)]
pub struct WorkspaceState {}
