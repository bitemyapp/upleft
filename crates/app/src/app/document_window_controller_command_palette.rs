//! Port of `App/DocumentWindowController+CommandPalette.swift`. Not ported yet.

/// The extension's associated-object state (`commandPaletteWindow`, `quickOpenProviders`), held by the
/// controller as `command_palette_state()`.
// PORT: DocumentWindowController+CommandPalette.swift fills this in.
#[derive(Default)]
pub struct CommandPaletteState {}
