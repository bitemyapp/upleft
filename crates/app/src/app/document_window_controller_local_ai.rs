//! Port of `App/DocumentWindowController+LocalAI.swift`. Not ported yet.

/// The extension's associated-object state (`localAIPanel`, `localAIProvider`, `localAICoordinator`), held by the
/// controller as `local_ai_state()`.
// PORT: DocumentWindowController+LocalAI.swift fills this in.
#[derive(Default)]
pub struct LocalAIState {}
