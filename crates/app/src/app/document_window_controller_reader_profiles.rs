//! Port of `App/DocumentWindowController+ReaderProfiles.swift`. Not ported yet.

/// The extension's associated-object state (`ReaderProfileControllerState`), held by the
/// controller as `reader_profiles_state()`.
// PORT: DocumentWindowController+ReaderProfiles.swift fills this in.
#[derive(Default)]
pub struct ReaderProfilesState {}
