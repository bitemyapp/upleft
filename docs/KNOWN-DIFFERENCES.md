# Known differences from Downright

This ledger lists every place where Upleft is known or suspected to behave differently from the Swift original. An entry leaves this file only when its difference is fixed and a conformance case covers it, or when the entry is recorded as an intentional, reviewed deviation.

## Open

| Area | Difference | Found by | Plan |
|---|---|---|---|
| Theme JSON decoding (`upleft-render` `swift_compat`) | Swift's `JSONDecoder` rejects some zero literals, such as `0e1` and `0.00e-5`, and the port accepts them. This only affects malformed theme files. | render-base port | Find Foundation's number grammar with a probe covering every literal shape, then match it. Add a malformed-theme corpus to the `vscode-theme` suite. |
| `upleft-math` `add_latex_symbol` across threads | The symbol table stores the registered atom itself, as Swift does, so a later change to it shows in lookups on the thread that registered it. Another thread gets a copy of the atom as it was when registered, because Rust can't share the caller's non-thread-safe cells. In Swift that cross-thread read is a data race unless the caller synchronises. Downright never calls `add(latexSymbol:)`. | math-variants | Keep. Covered by `crates/math/tests/add_latex_symbol_tests.rs`. |
| `.drBlock` and `.drPathToken` values (`upleft-render` `swift_value`) | Swift stores `BlockIdentity` and `PathToken` in `__SwiftValue` boxes; the port uses the NSObject subclasses `UpleftBlockIdentityValue` and `UpleftPathTokenValue`. `isEqual:` matches Swift's `Hashable` equality, so attribute runs coalesce identically (decorate 5454/5454), but the class names, `-description` and `-hash` values differ. | engine port | Keep unless something reads the class name. |
| Lone surrogates in the decoration engine | Swift reads a code block's text and a callout's source through `String`, which repairs a lone surrogate to U+FFFD before highlighting or searching; the port passes the UTF-16 units as they are. Unverified; no corpus document has a lone surrogate. | engine port | Add a lone-surrogate document to the corpus and compare. |
| `ClipboardSemanticHTML` substring search | Swift's `Substring.range(of:)` is Foundation's non-literal search; the port matches Character by Character, with Swift's canonical equivalence for single characters. The two agree on the corpus (clipboard 909/909). Unverified next to composed sequences such as `*` followed by a combining mark. | engine port | Add clipboard cases with combining marks after `*`, `_`, `~` and backticks. |
| `DecorationResult.elapsed` | Measured with a monotonic clock; Swift subtracts `CFAbsoluteTimeGetCurrent()` readings, which follow wall-clock changes. Only the timing differs. | engine port | Intentional. |
| ELK layering where elk-swift is nondeterministic (`upleft-elk`) | elk-swift iterates `NetworkSimplex.treeEdges`, a `Set` hashed by object address with a per-process seed, and breaks `HyperEdgeCycleDetector` ties the same way, so on some graphs its layout varies from run to run. None of the graphs captured from real Mermaid diagrams vary; 50 of the 449 corpus graphs (random and polyline stress graphs) do. Upleft always takes the insertion-order outcome. On 37 of the 50 that equals one of 16 sampled elk-swift runs. On 13 it equals only the instrumented elk-swift copy (`just elklab`), and whether plain elk-swift can produce those outcomes is unverified. The `elk` oracle emits `{"alternatives": [...]}` only for graphs whose samples disagree, and consults the instrumented copy only then. | elk port | Keep. Downright has no single output to match on these graphs. Watch for any Mermaid-derived graph that varies. |
| ELK compound preprocessor | The port clears the port-to-node entry list at the start of every layout; Swift never clears it. The stale entries only touch graphs from earlier layouts, so the output is identical. | elk port | Keep. |
| `DocumentHealth` footnote order | Swift walks footnotes in `Dictionary` order, which changes from run to run; the port walks them by position. No real parse can show the difference. | core port | Keep. Swift has no fixed order to match. |
| Atomic save temp names | Foundation's `.sb-` temp-file naming is only approximated, and its fallback for a directory that isn't writable is not ported. | core port | Port the naming exactly when the app layer lands. Check by saving into a directory that isn't writable. |
| Sorting by Swift `String <` (`upleft-swift-text` `str_cmp`) | Swift's `<` is not a strict weak order on strings that are not in NFC: when one string's NFC is a prefix of the other's, the string with fewer UTF-8 bytes is less, so `"a\u{301}"` and `"\u{E1}b"` are neither less nor equal. `str_less` reproduces `<` exactly (the `unicode` suite checks it), but Rust's `sort_by` over `str_cmp` can order such keys differently from Swift's sort, or panic where Rust detects the inconsistency. Real keys (link labels, identifiers, theme names) do not hit this. | swift-text | Port Swift's `sort(by:)` (its insertion sort and merge) into `upleft-swift-text` and use it wherever a port sorts by a `String` comparison. |
| `String` comparison on bridged strings | `str_less` models native Swift strings. A string bridged from an `NSString` skips the byte-prefix shortcut and compares remaining lengths in UTF-16 units, so the same two strings can order differently when one side is bridged. Not observed in any port. | swift-text | Add a `str_less_bridged` when a call site is found that compares a bridged string with `<`. |
| `StyleToken` (`upleft-render` `fragment_base`) | Swift folds the style values into its per-process seeded `Hasher`; the port uses Rust's `DefaultHasher` over the same values in the same order. The token is only compared within one process (cache invalidation), so no output can observe the difference. | view port | Keep. |
| `LocalAssetPolicy` URLs (`upleft-render` `local_asset_policy`) | Swift's `URL` keeps a directory URL's trailing slash through `resolvingSymlinksInPath()`; `NSURL` drops it. Path components, and so every allow/block decision, are identical on every probed destination (`..`, `a/../../b`, symlinks in and out of the folder, `~`, `file://`, fragments, percent signs, decomposed names). Only the `ImageRenderCache` key of a directory destination differs, and a directory never decodes. | fragments port | Keep. `local_asset_policy_tests` pins the probed destinations. |
| `ImageRenderCache` keys and freshness (`upleft-render` `bounded_image_cache`) | Swift keys decodes by `URL` and reads `.fileSizeKey`/`.contentModificationDateKey`; the port keys by the standardized URL's absolute string and `stat`s the path (`std::fs::metadata`). Same identity for every file URL; a file's size and modification date are the same values. | fragments port | Keep. |
| Mermaid in the render layer | `MermaidFragment` reaches the renderer through a hook that `upleft-mermaid` installs (`install_fragment_renderer`), because that crate depends on this one. A host that forgets to install it draws every diagram as a failed object; Swift has no such step. | fragments port | Keep; `upleft-oracle` installs it, and the app must (noted in `view/PORTING.md`). |
| Math and Mermaid on the main thread | As in Swift, a formula or diagram is typeset synchronously the first time layout asks its fragment for a height, so the first displayed frame contains it. AGENTS.md forbids blocking the main thread beyond what painting Swift's frames needs; rendering ahead on a worker would change which frame first shows the object. Both Rust renderers are faster than Swift's. | fragments port | Intentional (same threading as Swift). Revisit with an ahead-of-layout worker that still lands before the first frame. |
| `ThemeStore` hot reload | Rust reads the themes folder on a background queue during a hot reload. Swift reads it on the main thread. The loaded result is the same; only when it lands can differ. | render-base port | Intentional, per AGENTS.md: never block the main thread. The observable state after a reload must still match. |
| Sequence diagrams: open activations (`upleft-mermaid` `src_sequence_layout`) | Activations still open after the last message are appended in Swift `Dictionary` order, which is randomised per process, so Swift's own order varies between runs. Upleft appends them in first-activation order. Bars of different participants never overlap, so pixels are unaffected. The `mermaid-layout` dump compares activations sorted. | mermaid port | Intentional: Swift has no stable order to match. |
| `String.contains` provenance (`upleft-mermaid` `swift::contains`) | Swift's `contains` is Character-wise on a native string but Foundation's search on one bridged from an `NSString` (see `upleft-swift-text` `contains_bridged`). beautiful-mermaid makes many labels through `NSRegularExpression` and `NSString.substring`, so some are probably bridged. The port always searches Character-wise. The two can only disagree on a label where `\n`, `$` or `*` sits inside a grapheme cluster (for example `\r` then `<br>` in a flowchart label, giving `\r\n`). No corpus diagram does. | mermaid port | Probe the provenance of each `contains` call site's receiver with the corpus's adversarial labels and switch those sites to `contains_bridged`. |
| Feed probe threading (`upleft-app` `updater::release_watch`) | `ReleaseFeedURLProbe` classifies the response and hashes the body (up to 4 MiB of SHA-256) on the `URLSession` delegate queue, then hops to the main queue with the answer; Swift does both after resuming on the main actor. The answer and the order of main-queue work are the same (`updater` suite, `feed-probe*`). | updater port | Intentional, per AGENTS.md: never block the main thread. |
| `stateDidChange` object (`upleft-app` `updater::update_coordinator`) | Swift posts `Downright.UpdateCoordinator.stateDidChange` with the coordinator as `object`. The port's coordinator is not an Objective-C object, so it posts a per-coordinator `NSObject` stand-in, `notification_object()`; observers that filter by object must pass that. Name, timing and count of posts are identical (`updater` suite). | updater port | Keep; the UI port observes with `notification_object()`. |
| Release watch system events (`upleft-app` `updater::release_watch`) | `observeSystemEvents` (activation, power-state and wake notifications, `NWPathMonitor` through Network.framework's C API) is ported but unverified: neither the Swift tests nor the `updater` suite start a watch with system events, since their timing is live. | updater port | Verify in the running app once the UI port starts the coordinator: toggle activation, Low Power Mode, sleep/wake and Wi-Fi and compare probe counts with Downright. |
| Canonically equivalent keys in agent settings (`upleft-cli` `agent_bridge::bridged`) | `down hook --install`/`--uninstall` and `AgentIntegration` bridge a parsed `settings.json` object to a Swift `[String: Any]`, which merges keys that differ only in Unicode normalization (`"é"` and `"e\u{301}"`, `"K"` and KELVIN SIGN) into one entry. Which entry survives changes from process to process in Swift (probed: two runs of the same program disagree; it follows the per-process hash seed). Upleft keeps the last one in `NSDictionary` enumeration order. Nested objects the code never casts keep every key on both sides; the `down-cli` suite checks that (`hook-install-nested-canonical-duplicate-keys`). | cli port | Keep. Swift has no stable result to match. |
| `down` launch paths (`upleft-cli` `bin/down`) | The paths that start Downright or Finder are not in any conformance suite, because running them would launch an app: `open` and `notify` success (`locateApp`, `mdfind`, the `/usr/bin/open` arguments), `--reveal` success (`NSWorkspace.activateFileViewerSelecting`), `watch` success (FSEvents to `open`), and "could not find Downright.app" (depends on what `mdfind` finds on the machine). Ported line by line and unverified; `AgentWatcher`'s stream is covered by a Rust test that records instead of launching. | cli port | Add an injection point for the launcher if Downright grows one; until then, review by hand. |
| `down doctor` results depend on the machine | `codesign`, `spctl`, `xcrun stapler`, `pluginkit` and the default `.md` application are read from the machine, as in Swift. The `down-cli` suite caches Swift's result per scenario, so a change on the machine (a Downright.app installed in /Applications, a new Quick Look registration, another default Markdown app) makes the cached Swift results stale. | cli port | Rerun with `--no-cache` after such a change. |
| `KeybindingStore` persistence (`upleft-app` `support::keybindings`) | Swift writes `keybindings.json` synchronously from the thread that edits the store (the main thread, in Settings). The port encodes the bytes synchronously and writes them on a private serial queue, so `lastPersistenceError` is updated when the write finishes (`flush()` waits for it). The file is byte-identical (palette suite, 11 store cases). The store is a `Mutex`-guarded singleton, so its first load can run off the main thread. | palette port | Intentional, per AGENTS.md: never block the main thread on `fsync`. |
| `KeybindingLoad.decode` error choice (`upleft-foundation` `json_decoder`) | Swift decodes the entries of `[String: [KeyBinding]]` in per-process hash order, so when two entries would throw different errors (`keyNotFound`/`valueNotFound` 4865 vs. `dataCorrupted`/`typeMismatch` 4864), Swift reports either one and the user sees one of two messages. The port walks document order. Likewise, two override names that are canonically equivalent (`showKeybindings`, `show\u{212A}eybindings`) resolve to one command, and which binding list wins follows hash order in Swift and document order in the port. | palette port (probe) | Keep: Swift has no stable order to match. The palette corpus avoids such files. |
| `KeybindingStore.lastPersistenceError` | Held as the write error's text (`io::Error`), not an `NSError`; only its presence is observable (nothing in Downright displays it). | palette port | Keep unless a caller starts showing it. |
| Main-actor callbacks in `SystemIntegration`, `SpeechCoordinator`, `IntegrationRegistry` | Swift's `@MainActor` isolation becomes a `MainThreadMarker` parameter; completions (`registerWithSystem`, `makeDefaultMarkdownHandler`, `moveToApplications`' relaunch failure) are delivered on the main queue, as Swift's `DispatchQueue.main.async` / main-actor resumption delivers them. `makeDefaultMarkdownHandler` takes a completion instead of being `async`. Not exercised by a conformance suite (they change system state). | palette port | Keep; the UI port wires them. Unverified beyond compile and the `main_thread_objects` test. |
| `FileWatcher` rename search (`upleft-app` `ai::file_watcher` `locate`) | When the watched file disappears, the port looks for its inode among the directory's entries and skips any entry whose name is not valid UTF-8. Swift's `FileManager.contentsOfDirectory(atPath:)` returns such names in a decoded form. APFS and HFS+ only store valid Unicode names, so this can only matter on foreign volumes (SMB, FAT), and only when the renamed file itself has such a name. Unverified. | document port | Probe `contentsOfDirectory(atPath:)` on a volume that allows arbitrary bytes, then match its decoding. |
| Save-failure text (`upleft-app` `ai::markdown_document` `DocumentError`) | When a read, encode or write inside `MarkdownDocument` fails with a filesystem error, the `saveFailed` presentation detail, the `onSaveFailure` error and `SaveError.fileUnreadable`'s description show the Rust `io::Error` text (for example "No such file or directory (os error 2)"). Swift shows Foundation's `localizedDescription` ("The file “x.md” couldn’t be opened because there is no such file."). `DocumentIOError`, `PosixError` and `CocoaError(.fileNoSuchFile)` ("The file doesn’t exist.") already match. | document port | Map `io::Error` kinds to Foundation's `NSCocoaErrorDomain` file-error descriptions in `upleft-core` `document_io`, probing each code. |
| Parse control order on open (`upleft-app` `ai::markdown_document`) | Swift sends the coordinator's `resume`/`discardPending` through a chain of `Task`s, while the first-paint `runImmediately` comes from a separate detached task, so the two race and a reopened document can occasionally see its first priority parse refused as "suspended". The port sends all of these through the coordinator's FIFO queue in call order, so `resume` always lands first. | document port | Keep: Swift has no fixed order to match. |
| Persisted JSON key order (`upleft-app` `snapshot_store`, `document_state_store`, `reader_profiles`) | Snapshot indexes, state files, `recents.json` and the custom reader-profile file are written by a `JSONEncoder` without `.sortedKeys`, whose key order changes from process to process in Swift. The port writes `CodingKeys` order. The `formats` suite compares these files parsed. | app-persist port | Keep. Swift has no stable order to match. |
| `Set` member order in persisted files (`upleft-app` `document_state_store`, `trust_store`) | Swift writes `foldedHeadings`, `expandedCodeBlocks`, `collapsedCodeBlocks` and each grant's `effects` in per-process hash order. The port writes strings sorted by UTF-8 bytes, offsets ascending, effects in declaration order. `foldedHeadings` keeps Swift's set semantics (`StringSet`: canonically equivalent slugs are one member, the first spelling wins). | app-persist port | Keep. The `formats` suite sorts these members on both sides. |
| `SnapshotStore.waitForPendingWrites()` / `pruneOneGenerationForTesting()` (`upleft-app` `snapshot_store`) | Swift's are `async`; the port's block the caller until the history queue drains. `after_pending_writes` is the non-blocking form. | app-persist port | Intentional. Never call the blocking forms on the main thread. |
| `Preferences` load, recovery and persistence (`upleft-app` `preferences`) | Not covered by the `formats` suite: `Preferences.shared` is the only Swift entry to `read(contentsOf:)` and `persist()`, and it writes the user's global preferences (`PreviewAppearanceStore`), which `CFFIXED_USER_HOME` does not isolate. The suite checks `Preferences.Values` decoding and encoding byte for byte; the load path is covered by Rust tests only (`tests/preferences_load_tests.rs`). Unverified against Swift. | app-persist port | Cover it if the app oracle ever gets an isolated preferences domain. |
| `ChangeTracker` threading (`upleft-app` `change_tracker`) | The tracker holds `Rc` state and its fade timer runs on the main run loop, so it must live on the main thread; Swift's class can be created anywhere (its timer also fires on the main run loop). | app-persist port | Keep; MarkdownDocument owns it on the main thread. |
| `Date.iso8601` before Julian Day 0 (`upleft-foundation` `date`) | Dates before -4712-01-01T12:00:00Z print as that instant, as Swift printed a single probed point (-3e11 s). Other points are unverified. | app-persist port | Probe a range if a caller ever writes such dates. |
| `ExternalEditor.open` (`upleft-app` `path_resolver`) | Ported call for call (NSWorkspace, `Process`, `NSAppleScript`), but its side effects (launching editors, Terminal) are not exercised by any test or suite. Unverified. | app-persist port | Cover with the UI port's window-level checks. |
| `HTMLExporter` data URIs (`upleft-app` `export::html_exporter`) | The PNG data URI of a math or diagram image is remembered per `NSImage` object and drawing appearance (at most 64 images and 16 M characters), because the TIFF → `NSBitmapImageRep` → PNG encoding is nearly all of an export's time. Swift encodes again on every export. A remembered URI is the string the same calls produced for that image, so the HTML is identical (html-export 13440/13440, including a document that repeats a formula, a diagram and an image); only time and memory differ. | export port | Intentional (speed). Drop the memo if an image object is ever found to encode differently a second time. |
| Mermaid images in export (`upleft-app` `NativeFragmentImageProvider`) | Swift's `MermaidRendererBridge.image` caches in `MarkdownFragmentImageCaches.mermaid`, which fragments and export share. The Rust bridge is uncached, so the export provider keeps its own cache with the same limits (48 images, 24 MB) and a `StyleToken` hashed with Rust's hasher (Swift's `Hasher` is seeded per process, so only equality matters). Fragments and export don't share entries. | export port | Point the provider at the fragment layer's `MarkdownFragmentImageCaches.mermaid` when the view port has it. |
| `WorkspaceIndex` and `WorkspaceSearchSession` scheduling (`upleft-app` `workspace`) | Swift runs a scan in `Task.detached(priority: .userInitiated)` with `DispatchQueue.concurrentPerform` readers. The port runs it on the user-initiated global dispatch queue with scoped reader threads, then hops to the main queue as the `@MainActor` continuation does. The revision and cancellation checks are the same, so only timing differs. On both sides, when `maximumTotalBytes` is below the workspace's size and there is more than one reader, which file is skipped depends on scheduling (the `workspace` suite sets one reader for that case). | export port | Intentional. |
| `SetupWindowController` setup threading (`upleft-app` `app::setup_window_controller`) | Swift runs the whole setup in one main-actor `Task`, so the `isDefaultMarkdownHandler` re-check after the default-handler change, `SystemIntegration.installCommandLineTool()` (symlinks, `/etc/paths`) and `AgentIntegration.install()` (reads and writes `~/.claude/settings.json`) block the main thread. The port runs those three on the user-initiated global queue and reports each on the main queue before the next step starts, so the steps, the status texts and their order are the same; only when each label lands can differ. The move to Applications (trash and copy of the bundle) and the `makeIfNeeded()` checks still run on the calling thread, as in Swift; `applicable_steps()` lets a caller run the checks off the main thread. Unverified: no test runs the setup actions, because they change system state. | app-shell menu port | Intentional, per AGENTS.md: never block the main thread. |
| `CommandResponder` (`upleft-app` `app::main_menu`) | Swift declares an `@objc protocol` (by Swift's mangling its runtime name is `_TtP12DownrightApp16CommandResponder_`; not probed). The port registers no Objective-C protocol: `CommandResponder` is a Rust trait, and responders expose the `performDownrightCommand:` selector that menu items dispatch to. Nothing in Downright asks the runtime for the protocol. | app-shell menu port | Keep unless something checks `conformsToProtocol:`. |
| Menu item details (`upleft-app` `app::main_menu`) | A command item's `representedObject` is an `NSString` (`__NSCFString`); Swift stores a bridged Swift `String` (a Swift-native `NSString` subclass). `as? String` and `command(for:)` read both the same. `NSApp` is `NSApplication.sharedApplication`, which creates the application object when none exists, where Swift's `NSApp` would be nil and trap. `HelpLinkTarget` parses its URL with `NSURL(string:)` instead of Swift's `URL(string:)`; the four fixed links parse identically. | app-shell menu port | Keep. |
| Settings theme popups (`upleft-app` `PreferencesForms::appearance`) | Swift reads `ThemeStore.shared.themes` twice, once for the light list and once for the dark list. Its reads and its reloads all run on the main actor, so both lists come from one theme set. The Rust store can land a hot reload from its background queue between two reads, so the port reads the set once, which keeps that guarantee. | app-shell-prefs port | Keep. |
| Monospace family order (`upleft-app` `PreferencesForms::typography`) | Swift sorts `NSFontManager.availableFontFamilies`, strings bridged from `NSString`, with `<`. The port sorts with `str_less`, which models native strings (see "`String` comparison on bridged strings" above). The two orders are identical for ASCII family names; for non-ASCII names where one name's NFC form is a prefix of the other's, they are unverified. | app-shell-prefs port | Switch to `str_less_bridged` once it exists. |
| Settings tab-selection observer (`upleft-app` `app::preferences_window_controller`) | Swift observes `selectedTabViewItemIndex` through Foundation's `NSKeyValueObservation`. The port registers a private `NSObject` subclass, `PreferencesTabSelectionObservation`, with `addObserver:forKeyPath:options:context:` (`.new`, nil context), and removes it when the controller is released. Only the observer's class is different. | app-shell-prefs port | Keep. |
| Settings reads on the main thread (`upleft-app` `app::preferences_window_controller`) | As in Swift, a pane builds its rows on the main thread whenever it loads, appears or filters. Those rows read `Preferences`, LaunchServices (the default Markdown handler and the installed editors), the agent settings file, the font family list and `SnapshotStore.totalBytes()`, which walks the snapshot objects folder. The buttons write the command-line links and the agent settings synchronously, then report the result with a `runModal` alert. AGENTS.md asks for stricter threading than Swift's, but the rows are the pane's first frame, and reading them later would change what that frame shows. | app-shell-prefs port | Intentional: same threading as Swift. Revisit with a prefetch that lands before the pane's first frame, for example `totalBytes()` computed off the main thread when the window opens. |
| Window observations in `ToolbarDocumentIdentityView` (`upleft-app` `app::toolbar_controls`) | Swift's `window.observe(\.title, …)` registers Foundation's `NSKeyValueObservation` helper as the KVO observer. The port registers its own observer object (`UpleftKeyValueObservation`, `KeyValueObservation`) with the same key paths (`title`, `subtitle`, `documentEdited`, `representedURL`), the same options (`.initial`, `.new`), the observed window held weakly, and removal on deinit. Only the observer's class differs; nothing in Downright inspects it. | app-shell toolbar port | Keep. |
| Document path menu URLs (`upleft-app` `app::toolbar_controls`) | `showPathMenu` walks the represented URL's parents with Swift `URL` semantics (`upleft-foundation` `FileUrl`) and stores each parent as `NSURL(fileURLWithPath:isDirectory:)`, where Swift stores its bridged `URL`. The first entry is the window's own `representedURL.absoluteURL`, so `openPathComponent`'s equality test compares what Swift compares. Titles, icons and Finder paths are the same strings. Unverified: the menu tracks modally and its actions open Finder, so no test runs them. | app-shell toolbar port | Cover with the window-level checks once `DocumentWindowController` is ported. |
| Sparkle bridge (`upleft-app` `updater::sparkle`) | Downright links Sparkle 2.9.6 into its executable, so a bundle without the framework fails to launch. upleft-app looks Sparkle up in the Objective-C runtime instead (only the app binary links it): without the framework, the `SPUUpdater` factory answers `None` and the updater stays in its ordinary disabled state. Threading: Sparkle calls its user driver on the main thread; should a call arrive on another thread, Swift's `@MainActor` driver (Swift 5 mode, no dynamic isolation check) would run it there, while the bridge carries it to the main queue. For `updater:didDownloadUpdate:` off the main thread, Swift reads the display version and the handler on Sparkle's thread and the coordinator's handler hops to the main actor; the bridge hops first and reads both on the main thread, so the coordinator sees the same call on a later main-thread turn (`sparkle_bridge_tests`). | sparkle port | Intentional, per AGENTS.md: main-thread objects are never touched elsewhere. |
| Start window menu items (`upleft-app` `app::start_window_controller`) | A recent row's Show in Finder, Copy Path and Remove from Recents items carry the path as an `NSString` `representedObject`; Swift stores a bridged Swift `String`. `as? String` reads both the same (the `start_window_tests_window` row-menu test). | app-shell start port | Keep. |
| Start window reads on the main thread (`upleft-app` `app::start_window_controller`) | As in Swift, building the window reads `KeybindingStore.shared` (the shortcut hints; its first load reads `keybindings.json`), `URL(fileURLWithPath:)` checks whether each recent's path is a directory (row titles), `BrandMarkView` loads `Contents/Resources/AppIcon.png` on its first draw, and a drag over the window checks that each dropped file exists (`draggingEntered`/`draggingUpdated`). The first three feed the window's first frame; the drag must answer synchronously. | app-shell start port | Intentional: same threading as Swift. |
| Lightbox image load (`upleft-app` `app::document_window_controller_actions` `present_lightbox`) | Swift reads the image in `Task.detached(priority: .userInitiated)`, on the concurrency runtime's cooperative pool, and presents it through `MainActor.run`. The port reads it on the global user-initiated dispatch queue and presents it from a main-queue block. The priority, the order of steps and the main-thread presentation are the same; only the worker thread differs. | UI port (DWC `+Actions`) | Keep. |
| Error text in the document window's alerts (`upleft-app` `app::document_window_controller_support`) | "Couldn’t save a copy" shows the Rust `DocumentIO` error text, as the save-failure alerts already do (see "Save-failure text"). The export, selection-image and Continuity Camera writes go through `NSData` and show Foundation's `localizedDescription`, as in Swift; the trust alerts build `CocoaError(.fileWriteUnknown)` as an `NSError` and show its text. | document window port | Follow the save-failure fix when `DocumentIO` returns Foundation errors. |
| `DOWNRIGHT_DEBUG_LAYOUT` toolbar lines (`upleft-app` `app::document_window_controller::opening`) | Swift prints `view=\(String(describing: type(of: item.view)))`; the port prints `Optional<NSView>`, the static type Swift reports for an optional view. Unverified: the Swift line was not captured. The dump is debug output on stderr and is compared by no suite. | document window port | Capture one Swift dump and compare. |
| `[weak self]` in `Send` callbacks (`upleft-app` `app::document_window_controller`) | The theme observation, `PathResolver.warm`'s completion and the sibling-search result hop capture a `ControllerHandle` (an id resolved through a main-thread registry) instead of a weak reference to the controller, because the Rust closures must be `Send`. They run on the main queue as in Swift; a controller that has been released resolves to nothing, as a nil weak reference does. | document window port | Keep. |
| Restore-alert date (`upleft-app` `app::version_timeline_window_controller`) | The timeline's "Restore this version?" alert writes the version's date with Swift's `Date.FormatStyle(date: .abbreviated, time: .shortened)`, which has no Objective-C API. The port asks `NSDateFormatter` for the current locale's pattern for the skeleton `yMMMdjmm` and shortens each hour field to one letter (what ICU's hour-field-length matching does for `FormatStyle`). A Swift probe matched on 24 locales × 400 dates; other locales, calendars and numbering systems are unverified. | DWC extensions port | Extend the probe to every `Locale.availableIdentifiers` entry and fix any pattern that differs. |
| Share staging errors (`upleft-app` `app::document_window_controller_share`) | "Couldn’t prepare this document for sharing" shows the staging error as Rust formats it: `export::document_share` creates the share folder with `std::fs` and writes through `DocumentIO`, so a failing temporary directory reads as an `io::Error`, not as Foundation's `localizedDescription`. | DWC extensions port | Create the staging folder through `NSFileManager` in `export::document_share` so the error is an `NSError`. |
| Table editor sheet (`upleft-app` `app::document_window_controller_table_editor`) | The "Edit Table" sheet window has `releasedWhenClosed` turned off, which Swift leaves on: a window Rust holds through `Retained` must not release itself. The sheet is only ever ended (`endSheet`), never closed, so nothing observable changes. | DWC extensions port | Intentional (memory safety). |
| Private classes of the DWC extensions (`upleft-app` `app::document_window_controller_command_palette`, `…_reader_profiles`) | Swift's `private final class CommandPaletteWindowDelegate` has a mangled runtime name (`_TtC12DownrightApp…`); the port's class is named `CommandPaletteWindowDelegate`, per the UI port's naming rule. `ReaderProfileControllerState` is an `NSObject` subclass in Swift only so it can be an associated object; the port's is a plain Rust struct held by the controller, never seen by AppKit. | DWC extensions port | Keep. |
| `UPLEFT_HEADLESS_SMOKE` (`upleft` binary, `smoke.rs`) | Mirrors nothing in Swift. With the variable set to a Markdown file, the app builds what a launch builds (main menu, Sparkle bridge, a document window over the file) with the activation policy `.prohibited`, never activates, never orders a window in, prints a JSON summary and exits. Used by the bundle check only; without the variable the start-up is main.swift's. | app-shell port | Keep (test hook, sanctioned by the coordinator). |
| App start-up (`upleft` binary, `main.rs`) | Before main.swift's sequence the Rust app installs the Mermaid renderer hook (`install_fragment_renderer`, see the Mermaid row above) and the Sparkle bridge (`updater::sparkle::install`), which Swift gets by linking the two frameworks directly. No behaviour differs. | app-shell port | Keep. |
| `Upleft.app` layout (`scripts/bundle-upleft-app.sh`) | Follows `Scripts/bundle-app.sh` with three deliberate differences: the bundle is never registered with Launch Services (Downright's script runs `lsregister -f`); the built-in themes are compiled into the binary, so there is no `Downright_MarkdownRender.bundle`; the math fonts ship as `Contents/Resources/mathFonts.bundle` (upleft-math's resolver) rather than inside `SwiftMath_SwiftMath.bundle`. The Info.plist comes from the rebranded `Config/Downright-Info.plist` (the Xcode template) with the Sparkle keys dropped in dev bundles, where bundle-app.sh writes its own plist. | app-shell port | Keep. |
| `AppDelegate` details (`upleft-app` `app::app_delegate`) | The effective-appearance observation is a small NSObject KVO observer named `UpleftAppearanceObservation` (Swift's `NSKeyValueObservation` is Foundation's own class). `--downright-demo-update` is parsed but inert, as in a release build (Swift's demo is `#if DEBUG`). `session.json` members are written in `CodingKeys` order (Swift's `JSONEncoder` without `.sortedKeys` orders them per process). The tour's `WelcomeTour.Error` text is Swift's generic `localizedDescription` spelled out ("The operation couldn’t be completed. (DownrightApp.WelcomeTour.Error error N.)"), unverified against a Swift probe. | app-shell port | Keep. |
| Window-level conformance harness (`app-window`, both oracles) | To keep titled windows off every display, both oracles replace `-[NSWindow constrainFrameRect:toScreen:]` with the identity for the whole process and refuse to continue if any visible window touches a display; ScreenCaptureKit cannot capture a window on no display, so both capture with `CGWindowListCreateImage` (looked up at run time). The same calls on both sides; the app itself is unchanged. Where Downright's own result depends on a race (Docs__sample's document window settles on one of two estimated heights, depending on whether the asynchronous parse or the first-frame restore lands first; both sides produce both, byte for byte), the runner collects four Swift outcomes and gives Rust three retries. | app-shell port | Keep. |
| Quick Look principal classes (`scripts/bundle-upleft-quicklook.sh`) | The extensions' `NSExtensionPrincipalClass` is `PreviewViewController` / `ThumbnailProvider`. Downright's Info.plists say `$(PRODUCT_MODULE_NAME).PreviewViewController`, which Xcode and bundle-quicklook.sh turn into `DownrightQL.PreviewViewController` (a Swift class in module DownrightQL). The Rust classes are registered under the Swift classes' unqualified names (AGENTS.md), and the Objective-C runtime resolves `DownrightQL.PreviewViewController` only to that name or its Swift mangling, so the script drops the module prefix. Every other Info.plist value is Downright's, rebranded. Unverified: whether the system's extension host loads the class by this name (the extensions are never registered or launched here). | Quick Look port | Keep; confirm on a machine where the extensions may be registered. |
| Quick Look extension resources (`scripts/bundle-upleft-quicklook.sh`) | Each Downright `.appex` carries `Downright_MarkdownRender.bundle` (themes) and `SwiftMath_SwiftMath.bundle/mathFonts.bundle` at its root. Upleft's carry `mathFonts.bundle` at the root, beside the executable, where upleft-math's resolver looks; the themes are compiled into the binary. The thumbnail extension carries the fonts too, as Downright's does, though it never typesets math. | Quick Look port | Keep (the same choice as the app bundle). |
| Quick Look preview parse (`upleft-quicklook` `preview_view_controller`) | Swift's `present` and `presentTruncated` run `MarkdownParser.parse` (and the structure-only parse of the prefix path) on the main actor, after the detached load. The port runs the same parses, with the same inputs and in the same order, at the end of the load on the user-initiated queue, and hands the documents to `present` on the main thread. Everything after the parse (storage, text view, gutter, bar, handler) happens on the main thread in Swift's order. A superseded preview can now spend its parse time before it notices the cancellation, where Swift would never parse it. | Quick Look port | Intentional, per AGENTS.md: never block the main thread with parsing. |
| `BreadcrumbView` zoom menu (`upleft-app` `panels::breadcrumb_view`) | Swift stores each zoom item's `ZoomLevel` (a `__SwiftValue` box) as `representedObject`; the port stores its raw value as an `NSNumber`. Only the view reads it. | panels port | Keep. |
| `QuickLookSession` (`upleft-app` `panels::document_quick_look`) | Swift's is a plain Swift class stored as an associated object; the port's is an `NSObject` subclass named `QuickLookSession`, stored the same way. Only the runtime class name differs. | panels port | Keep. |
| `MarkdownLinkDestination` in `panels::document_quick_look` | The module carries a private copy of `MarkdownLinkDestination.classify` (App/`DocumentWindowController+Delegates.swift`), which the app shell has not ported yet. | panels port | Switch to the app shell's type when that file lands. |
| Update notes HTML entities (`upleft-app` `panels::update_notes_popover`) | Swift's `textFromHTML` decodes the six named entities in `Dictionary` order, which changes per process, so double-escaped input such as `&amp;amp;lt;` gives `&lt;` or `&amp;lt;` depending on the run. The port decodes in the literal's order. | panels port | Keep: Swift has no stable result. |
| Task rows' default style sheet (`upleft-app` `panels::task_panel_view`) | Swift builds a full `StyleSheet.current` for every new row and overwrites it before it is read; the port builds it only if read. No output can see the difference; it was a quarter of the panel's build time. | panels port | Intentional (speed). |
| `CommandPaletteModel` sorts (`upleft-app` `support::command_palette_model`) | Each title is bridged to `NSString` once per sort instead of per comparison; the comparisons (`localizedStandardCompare`) and so the order are unchanged (palette 23/23). | panels port | Intentional (speed): opening the palette over agent-5000 was 30% slower than Swift before. |
| Front matter `Double(text)` (`upleft-app` `panels::front_matter_editor_view`) | Swift's `Double(String)` is reproduced as `swift_double_parses` plus a C-locale `strtod_l`; `snan` becomes a quiet NaN where Swift's is signalling (both display "nan"). Only "12" is exercised by the conformance scenarios. Unverified. | panels port | Probe Swift's `Double(_:)` over the special spellings. |
| `RenderTargetProfile` names (`upleft-core`) | Swift compares custom profile names by canonical equivalence; the port compares bytes, so two names differing only in Unicode normalization are equal in Swift and not in the port (`selectedProfile`'s change check, the popup lookup). Found by reading, unverified. | panels port | Compare with `upleft-swift-text`'s `str_eq`, then add a render-targets scenario with such names. |
| Non-finite `Int(x)` in panels | `Int(measureCharacters)` (reader profile picker) and `Int(timeIntervalSince)` (version timeline tooltip) trap in Swift on non-finite or out-of-range values; Rust's `as` saturates. Unreachable with real data. | panels port | Keep. |
| Markdown parser (`upleft-markup`) | Upleft parses with pulldown-cmark 0.13.4 (CommonMark 0.31); Downright's swift-markdown parses with cmark-gfm (CommonMark 0.29 plus GFM). The adapter rebuilds cmark-gfm's tree and ranges wherever the parsers agree; the cases where they genuinely disagree are listed under "Markdown parser" below. | parser replacement (owner's decision, 2026-09-23) | Accepted. Keep the differential tool's counts at the baseline in docs/VALIDATION.md. |

## Markdown parser

By the owner's decision (2026-09-23) Upleft parses with pulldown-cmark instead of linking cmark-gfm's C sources. `upleft-markup`'s adapter reproduces cmark-gfm's conventions: paragraphs in tight list items, consolidated text nodes, delimiter-run extents, columns measured in prefix-stripped paragraph text, `finalize()`'s end positions, the table extension's cell offsets and spans, the tasklist pattern, inline attributes, and cmark's handling of NUL, CR and a byte order mark. What remains are inputs the two parsers read differently. None occurs in the corpus: `markup` and `parse` are 915/915. The `incremental` suite parses edited texts, and nine of its documents (27 cases) hit one of the rows below.

Counts come from `examples/markup_diff.rs --incremental --mutations 20000 --random 20000 --minimize` (2026-09-23): 443 differing inputs out of 47,960, each shrunk to a minimal repro and assigned to one cause. "Incremental" counts are corpus documents after one of the `incremental` suite's edits.

| Difference | Minimal example | cmark-gfm (Downright) | pulldown-cmark (Upleft) | Inputs (incremental / mutated / random) | Effect downstream |
|---|---|---|---|---:|---|
| A table header line as a lazy continuation | `>t` `\|\|` `>-\|` (three lines) | The unprefixed `\|\|` continues the quote's paragraph; the next quoted line makes it a table in the quote | A line that could start a table is not a lazy continuation: the quote ends, `\|\|` is a paragraph, a second quote follows | 0 / 0 / 121 | Those lines land in different blocks: quote bar, list indentation and table layout differ |
| One-column tables without pipes | `]` `-:` | Table (header `]`, one column) | Paragraph | 0 / 33 / 18 | Downright draws a table, Upleft a paragraph |
| What ends a table | `a\|\|` `-\|-` `    e` | A line indented four columns (or a tab), or an HTML tag line, ends the table: code block or HTML block | Another table row | 0 / 38 / 2 | An extra row in Upleft's table instead of a code or HTML block |
| Inline attributes beyond the rebuilt forms | `^[a]:-`, `^[][]`, `^[[]()]()`, `^[](<n>1)` | Attribute definition, attribute reference, or a span whose text holds brackets, line breaks or tags | Text and links. The adapter rebuilds `^[text](attributes)` when pulldown-cmark reads it as a link after `^`, or as one run of text | 0 / 21 / 11 | Text styling of the span; Downright gives inline attributes no decoration of their own |
| A table after a paragraph line, header without a leading pipe | `x` `a\|\|` `-\|-` | The table interrupts the paragraph | One paragraph. (When the header does start with `\|`, both interrupt, and the adapter reproduces cmark's positions for the interrupted paragraph, except that cmark also unescapes `\|` in it, shifting later columns on the line, and leaves a link reference definition in it as text) | 0 / 26 / 3 | Downright draws a table, Upleft a paragraph |
| A task box with no text, then a less indented line | `- [x]` `g` | Not a task (the pattern needs a space after the box); `g` continues the item's paragraph: `[x] g` | A checked task item with no text, then a paragraph `g` after the list | 12 / 10 / 5 | Checkbox drawn in Upleft only; `g` outside the list |
| An HTML tag line right after a list item or quote paragraph | `- m` `<n>` | HTML block after the list (cmark tests the container, not the open paragraph, for "type 7 cannot interrupt a paragraph") | Lazy continuation: inline HTML inside the item | 5 / 16 / 6 | Block HTML styling versus an inline tag in the item |
| Delimiter-row syntax; retrying a table | `a\|\|` `-\|:`; `\|` `-\|` `\|\|\|` `-\|-` | A cell of only `:` is not a delimiter; once a paragraph failed to become a table (`CMARK_NODE__TABLE_VISITED`) cmark never tries again | Accepts `:`; tries each header and delimiter pair | 0 / 24 / 0 | Table versus paragraph |
| Link reference definitions | `[a]:(`; `[bar]:l` then `"r"[bar]` | `(` is a destination; a title followed by text still counts | Paragraph text; the title is dropped | 0 / 5 / 14 | A definition line shown as text; a link without its title |
| Single and double tildes next to letters or punctuation | `a~e~`, `~t~x`, `y~~&~~` | Strikethrough | Text | 7 / 8 / 3 | Strikethrough drawn in Downright only |
| Emoji next to emphasis delimiters | `a*😀*` | Emphasis (Unicode symbols are not punctuation in 0.29) | Text (they are punctuation for flanking in 0.31) | 0 / 7 / 7 | Emphasis versus literal asterisks |
| HTML declarations with a lowercase name | `<!doctype html>`, `o<!A>` | Text (`<!` needs an uppercase letter, and a space after the name inline) | HTML block, or inline HTML | 8 / 3 / 0 | HTML styling versus paragraph text; `<!doctype html>` at the top of a file is the realistic case |
| Other emphasis delimiter matching | `**f*o****` | `**fo***` as text around emphasis | Different nesting | 0 / 3 / 5 | Which characters are bold or italic |
| Link destinations with parentheses | `[[]()[]]()`; `[js](JavaScript:alert(1 ))` | A link inside a link; a destination that stops at the space | Text around one link; text | 6 / 1 / 0 | Link spans differ |
| An empty item, a whitespace line, indented text | `-` `  ` `  n` | The whitespace line, indented to the item's content, keeps the item open, and `n` joins it | The item ends; `n` is a paragraph after the list | 0 / 0 / 7 | Indentation of `n` |
| Code spans after an unmatched backtick run | `` `` `e` `2` `` | `e` is code, `` `2` `` is text (cmark's backtick cache) | Both are code | 0 / 6 / 0 | Code styling of the second span |
| A tab after a quote marker line | `>` then a tab and `>` | Code block after the quote | Part of the quote | 0 / 1 / 0 | Code block versus quote |
| `![^` | `![^](x)` | Not an image (cmark-gfm keeps `[^` for footnotes): `!` then a link | Image | 0 / 1 / 0 | Link versus image |

## Reproduced on purpose

These are Downright behaviours that look like bugs. Upleft reproduces them because pixel-exactness requires it.

| Behaviour | Where |
|---|---|
| `SmartPaste.plainText(forHTML:)` never returns on input such as `a < b`. The port reproduces the hang. | `upleft-core` SmartPaste |
| swift-markdown measures every table row as the width of the first row when it pads column alignments. cmark never produces rows of different widths, so real input is unaffected. | `upleft-markup` tables |
| An xychart whose values or range approach `1e300` overflows its nice ticks to infinity, and the renderer's `Int((xBase / 20).rounded())` traps: Downright crashes on such a fence. Upleft panics at the same point. The two inputs live in `corpus/mermaid-traps/` (read by no suite); a test checks the panic. | `upleft-mermaid` `diagram_renderer_xy_chart` |
| beautiful-mermaid's flowchart clipping compares shapes against `"stateStart"`/`"stateEnd"`/`"stateFork"`, but positioned nodes carry `"state-start"`/`"state-end"`, so edges into start and end states are clipped as ellipses. | `upleft-mermaid` `src_layout` |
| After shifting a flowchart right or down to fit an edge label, only the top-level subgroup rectangles move; nested subgraph rectangles keep their old coordinates. | `upleft-mermaid` `src_layout` |
| `BMColor(hex:)` treats any length other than 6 or 8 hex digits as opaque black, so `style A fill:#f9f` and named colours such as `red` fill black. | `upleft-mermaid` `cross_platform` |
| `down export` traps (`SIGTRAP`, no message) on an image whose percent-decoded source leaves nothing before the query once split at `#`, such as `![x](#)`, `![x](?)`, `![x](?#b)` or `![x](\t)`: `imageSourceIsSafe` indexes `[0]` into an empty `split` result. Upleft traps the same way (`brk #1`). The `down-cli` suite covers it (`export-image-fragment-crash`). | `upleft-cli` `markdown_cli` |
| `KeyBinding.key(for:)` never produces `"backslash"`: it lowercases the event's characters, so ⌘\\ arrives as `"\\"` and misses the default Split View binding (`KeyBinding("backslash", .command)`). The chord works only through the menu item's key equivalent. The palette suite's store cases check the miss. | `upleft-app` `support::commands`, `support::keybindings` |
| Copies of `CommandPaletteModel` share one result cache (the Swift struct holds a class instance), so a copy can be handed results cached by another copy for the same query. `Clone` shares the cache the same way. | `upleft-app` `support::command_palette_model` |
| A save issued in the same main-queue turn as an edit leaves `MarkdownDocument` dirty again: the edit's `NSTextStorageDelegate` callback runs a turn later, finds the parsed tree still older than the buffer, and calls `setDirty(true)`. In the app a save normally comes turns after the keystroke, so this only shows when code edits and saves back to back. Worked out from MarkdownDocument.swift; not checked with a Swift probe. | `upleft-app` `ai::markdown_document` |
| `HTMLExporter.escape` walks Characters, so `<`, `>`, `&` or `"` followed by a combining mark is one Character that is none of them and ships unescaped (`<\u{338}` stays a raw `<`). | `upleft-app` `export::html_exporter` |
| The workspace link graph's `normalize` trims `.` and `/` but never resolves `..`, so a relative link that climbs out of its folder resolves only through the stem fallback; a wikilink resolves relative to the linking file's folder; and the stem keys come from `URL(fileURLWithPath:)` of the relative path, resolved against the current directory (`/` in a launched app, and in the `workspace` dump). | `upleft-app` `workspace::workspace_link_graph` |
| The workspace index collects links from each block's own inlines, so links inside table cells are never indexed or resolved. | `upleft-app` `workspace::workspace_index` |
| A workspace entry's relative path removes every occurrence of the root path plus `/` from its standardized path, not just the leading one. | `upleft-app` `workspace::workspace_index` |
| Settings leaks its form controls. Every toggle, stepper, stepper field, popup and text field keeps its `ActionHandler` as an associated object, and that handler's closure holds the control strongly, which makes a retain cycle. Each pane rebuild (every appearance, every search keystroke) therefore leaks the previous rows. The port's closures hold the same strong references. | `upleft-app` `app::preferences_window_controller` (`PreferencesPane`) |
| `BrandMarkView` passes `[.interpolation: NSImageInterpolation.high]` as the icon's drawing hint, which Swift bridges to a `__SwiftValue` box rather than an `NSNumber` (probed: the value answers no `integerValue`), so AppKit cannot read it and the icon draws with the context's interpolation. Upleft passes no hints. Unverified in pixels: neither oracle has a bundle with `AppIcon.png`, so the mark draws nothing in the `app-window` suite. | `upleft-app` `app::start_window_controller` (`hero::BrandMarkView`) |
| The keyboard layer's handler (`wireKeyEventHandler`) captures its text view strongly, and the text view stores the handler, so every document text view leaks. The port keeps the cycle. | `upleft-app` `app::document_window_controller_commands` |
| Tidy Document with nothing to do says "This markdownDocument already follows every rule Tidy checks." (a rename left in Downright's alert text). | `upleft-app` `app::document_window_controller_commands` |
| `PanelSegmentedControl(items:selectedIndex:styleSheet:)` reads the unclamped `selectedIndex` parameter (it shadows the clamped property) for its accessibility value, so an out-of-range index traps; the port panics at the same point. | `upleft-app` `panels::panel_chrome` |
| `DocumentStatusBarView`'s unsaved dot is never hidden, and the label's leading constraint is chosen once in `init` (with `hasFileURL` still true), so clearing `hasFileURL` changes only the text prefix. | `upleft-app` `panels::document_status_bar_view` |
| `CommandPaletteRowView` uses `FuzzyMatcher`'s character positions as UTF-16 offsets when tinting matches, so a title with astral or combined characters before a match tints the wrong units. | `upleft-app` `panels::command_palette_view` |
| `TableEditorView.tableView(_:didClick:row:)` matches no `NSTableViewDelegate` requirement, so header clicks never select a column; and `PanelList.makeTableView` removes the header view, so the column titles `rebuildColumns` sets are never drawn. | `upleft-app` `panels::table_editor_view` |
| `FrontMatterFieldRow.commit` checks `Double(stringValue)` on the untrimmed text (" 3 " as a Number shows "Enter a number."), `parseList` splits on every comma including quoted ones, `focusField(named:)` with no matching key focuses the add form, and moving focus between value fields sends the first field's action (a `.set` with the unchanged value). | `upleft-app` `panels::front_matter_editor_view` |
| `TidyProposalRowView.configure` decides whether a diff is truncated at the row's current width (the 200pt floor for a new row, the previous width for a reused one) and swaps the expand button's configured symbol for an unconfigured one. | `upleft-app` `panels::tidy_sheet_view` |
| In the downloading state the update window's header is bounded only from below and takes the free height; the content container collapses to 0pt, so the progress bar and byte counts are invisible. An animated popover dismissal releases the popover before its fade ends, so `close()` never runs and the transparent panel stays attached (worked out from source). | `upleft-app` `panels::update_window_controller`, `panels::update_notes_popover` |
| `WorkspaceSidebarView` groups files by `URL(fileURLWithPath: relativePath).deletingLastPathComponent().path`, resolved against the working directory, so in an app (cwd `/`) root-level files group under "/" and the "This folder" branch never matches (probed). A negative row index traps in `heightOfRow`/`isGroupRow`/`shouldSelectRow`; the port panics. | `upleft-app` `panels::workspace_sidebar_view` |
| `HistoryInspectorView`'s `styleSheet` `didSet` never updates its backdrop; `LocalAIPanelView.init` never calls `updateResult()`, so the preview stays empty until a result is assigned; `ReaderProfilePickerView`'s Revert stays enabled from `init` until the first selection. | `upleft-app` `panels::{history_inspector_view, local_ai_panel_view, reader_profile_picker_view}` |
| `SearchResultsPanelView` shows no status text or empty state until `query`, `hits`, `searchedFileCount` or `isSearching` is first set (`init` never calls `updateStatus()`); a negative row traps. | `upleft-app` `panels::search_results_panel_view` |
| With a task row selected, an edit that shortens the task list below that row's index traps in `TaskPanelView.preferredAddSection` (checked: the Swift oracle dies with SIGTRAP on the same sequence; the port panics). `focusAddField` looks up the add row a turn later at the old index, so NSTableView raises if the rows shrank meanwhile; negative indices in the drag paths trap (unverified); row heights are measured before the panel has its final width, so rows can be taller than their labels need (visible in captures). | `upleft-app` `panels::task_panel_view` |
| `AssetDiagnosticRowView` ignores its hover argument (the actions glyph stays at alpha 1) and never reads its stored `reduceMotion`. | `upleft-app` `panels::asset_doctor_view` |

## Retracted

- **"Downright clips the text column on first open."** This was never Downright's behaviour. It came from SwiftPM stamping the binary `sdk 14.0`, which put AppKit into an older compatibility mode. With the canonical build version (docs/BUILD-VERSION.md), the text view takes its full width.
