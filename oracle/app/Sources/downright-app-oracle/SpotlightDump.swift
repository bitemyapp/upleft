import Foundation
@testable import DownrightApp

/// Swift side of the `Spotlight` suite. Not written yet.
enum SpotlightDump {
    static func run(input: URL, flags: [String]) throws -> JSON {
        throw AppOracleError(description: "SpotlightDump is not written yet")
    }
}
