import Foundation
@testable import DownrightApp

/// Swift side of the `LocalAI` suite. Not written yet.
enum LocalAIDump {
    static func run(input: URL, flags: [String]) throws -> JSON {
        throw AppOracleError(description: "LocalAIDump is not written yet")
    }
}
