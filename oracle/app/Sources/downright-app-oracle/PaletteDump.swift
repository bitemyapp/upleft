import Foundation
@testable import DownrightApp

/// Swift side of the `Palette` suite. Not written yet.
enum PaletteDump {
    static func run(input: URL, flags: [String]) throws -> JSON {
        throw AppOracleError(description: "PaletteDump is not written yet")
    }
}
