import Foundation
@testable import DownrightApp

/// Swift side of the `DownCLI` suite. Not written yet.
enum DownCLIDump {
    static func run(input: URL, flags: [String]) throws -> JSON {
        throw AppOracleError(description: "DownCLIDump is not written yet")
    }
}
