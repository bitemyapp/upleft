import Foundation
@testable import DownrightApp

/// Swift side of the `Updater` suite. Not written yet.
enum UpdaterDump {
    static func run(input: URL, flags: [String]) throws -> JSON {
        throw AppOracleError(description: "UpdaterDump is not written yet")
    }
}
