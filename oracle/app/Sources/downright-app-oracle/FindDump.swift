import Foundation
@testable import DownrightApp

/// Swift side of the `Find` suite. Not written yet.
enum FindDump {
    static func run(input: URL, flags: [String]) throws -> JSON {
        throw AppOracleError(description: "FindDump is not written yet")
    }
}
