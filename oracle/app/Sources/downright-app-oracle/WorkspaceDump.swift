import Foundation
@testable import DownrightApp

/// Swift side of the `Workspace` suite. Not written yet.
enum WorkspaceDump {
    static func run(input: URL, flags: [String]) throws -> JSON {
        throw AppOracleError(description: "WorkspaceDump is not written yet")
    }
}
