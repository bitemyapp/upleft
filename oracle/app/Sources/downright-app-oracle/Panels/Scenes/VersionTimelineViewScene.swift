import AppKit
@testable import DownrightApp
import MarkdownCore
import MarkdownRender

/// `VersionTimelineView` scenes. Not written yet.
@MainActor
final class VersionTimelineViewScene: PanelScene {
    func build(_ scenario: PanelScenario, styleSheet: StyleSheet) throws -> NSView {
        throw PanelHarnessError.notPorted("VersionTimelineView")
    }
}
