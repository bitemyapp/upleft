import Foundation
@testable import DownrightApp

/// Swift side of the `Formats` suite. Not written yet.
enum FormatsDump {
    static func run(input: URL, flags: [String]) throws -> JSON {
        throw AppOracleError(description: "FormatsDump is not written yet")
    }
}
