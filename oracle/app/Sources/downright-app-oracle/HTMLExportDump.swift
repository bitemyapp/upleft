import Foundation
@testable import DownrightApp

/// Swift side of the `HTMLExport` suite. Not written yet.
enum HTMLExportDump {
    static func run(input: URL, flags: [String]) throws -> JSON {
        throw AppOracleError(description: "HTMLExportDump is not written yet")
    }
}
