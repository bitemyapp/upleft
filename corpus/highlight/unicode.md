# Non-ASCII code

```swift
let 名前 = "日本語"; let emoji = "👩‍👩‍👧‍👦 🇯🇵 é" // コメント
struct Größe { var wert: Int = ５ }
let 𝔘 = 1; let ok = '😀'
```

```rust
let ch = '😀'; let astral = '𝔘'; let ascii = 'a'; fn ünïcödé() {}
let lifetime_like = 'é;
```

```python
café = "naïve"; ÉTAT = 1; λ = lambda x: x
```

```css
.naïve::before { content: "→"; --größe: 1em; }
```

```html
<p title="café">Grüße &eacute; 😀</p>
```

```markdown
# Überschrift
*betonung* und [Verknüpfung](https://例え.jp)
```
